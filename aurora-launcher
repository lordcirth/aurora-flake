export WINEPREFIX=$HOME/.wine-aurora
export WINEARCH=win32
EXE="./Aurora.exe"
COPYDIR="$HOME/wine-aurora"

# Aurora saves in PWD, so we cd to SAVEDIR
cd $COPYDIR

# Wine path added in flake.nix
$WINE $EXE
